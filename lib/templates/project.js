function projectTemplate(db, dbKeys, TYPE)
{
    let html = "";

    var i = 0;
    while (i < dbKeys.length) 
    {
      //this.buildEntry(db, dbKeys[i]);
      //for every entry that belongs to this project, generate html
      i++;
      console.log(i)
    }

    return html;
}